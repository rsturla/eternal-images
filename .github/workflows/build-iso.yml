name: Build ISO Files

on:
  workflow_dispatch:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      IMAGE_REGISTRY: ghcr.io/${{ github.repository_owner }}/eternal-linux
    strategy:
      fail-fast: false
      matrix:
        image: [lumina]
        version: [39]
        arch: [x86_64]
        driver: ['', nvidia]
    steps:
      - name: Generate ISOs
        uses: rsturla/eternal-iso-generator@main
        with:
          arch: ${{ matrix.arch }}
          image-name: ${{ matrix.image }}
          image-tag: ${{ matrix.version }}${{ matrix.driver && format('-{0}', matrix.driver) || '' }}
          version: ${{ matrix.version }}
          image-repo: ${{ env.IMAGE_REGISTRY }}

      - name: Upload ISO
        uses: actions/upload-artifact@5d5d22a31266ced268874388b861e4b58bb5c2f3 # v4
        with:
          name: ${{ matrix.image }}-${{ matrix.version }}${{ matrix.driver && format('-{0}', matrix.driver) || '' }}-${{ matrix.arch }}-iso
          path: output/*.iso
          retention-days: 7
